import { MedusaRepository } from "@medusajs/framework/utils"
import { Fabric } from "../models/fabric"

export class FabricRepository extends MedusaRepository {
  // Custom repository methods can be added here if needed
  // The base MedusaRepository provides standard CRUD operations
}